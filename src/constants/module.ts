export enum ModuleContentType {
  Video = 'Video',
  Audio = 'Audio',
  Document = 'Document',
  Link = 'Link',
  Image = 'Image',
}

export enum ModuleContentStatus {
  Draft = 'Draft',
  Published = 'Published',
}
